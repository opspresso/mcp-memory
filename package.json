{
  "name": "mcp-memory",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "dev": "tsx src/main.ts",
    "test": "node --import tsx --test \"src/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-agent-runtime": "^3.1106.0",
    "@aws-sdk/client-bedrock-runtime": "^3.1100.0",
    "@aws-sdk/client-s3": "^3.1096.0",
    "@aws-sdk/client-s3vectors": "^3.1096.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.20.0",
    "typescript": "^5.9.3"
  }
}
